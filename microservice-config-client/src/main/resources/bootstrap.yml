spring:
  cloud:
    config:
      uri: http://localhost:8080
      profile: dev
      label: master   #当config server的后端存储的是git时，默认就是master
  application:
    name: foobar


#先加载bootstrap.* ,再加载远程配置，再加载application.*
